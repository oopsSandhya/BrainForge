server:
  port: 8080
spring:
  application:
    name: cognitive-fitness-platform
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/cognitive_fitness}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:Sandhya@1432}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:sandhyacgu@gmail.com}
    password: ${MAIL_PASSWORD:chbispmyfljtbjoc}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  data:
    redis:
      host: ${REDIS_HOST:emerging-prawn-150623.upstash.io}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:gQAAAAAAAkxfAAIgcDEwZDE2MmViYzY4OWI0YTE1OGIwNjE1M2Y4ZmFmNThiYw}
      ssl:
        enabled: true
  cache:
    type: redis
    redis:
      time-to-live: 60000
jwt:
  secret: ${JWT_SECRET:mySecretKeyForDevelopmentOnlyPleaseChangeInProduction}
  expiration: ${JWT_EXPIRY_MS:86400000}
app:
  frontend-url: ${FRONTEND_URL:http://localhost:5173}
logging:
  level:
    com.cognitivefitness: DEBUG
    org.springframework.security: DEBUG